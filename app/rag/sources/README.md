# Knowledge base source documents
# Place architecture papers, production case studies, and benchmark reports here.
# Only verified, published sources are allowed — no anecdotal or unverified material.
#
# Supported formats: .txt, .md, .pdf (via extraction)
# Ingest via: python -m app.rag.knowledge_base ingest <path>
