# Production requirements — mirrors [project.dependencies] in pyproject.toml.
# Kept as an explicit file because some PaaS providers (notably Render's
# default Python build) install more reliably from requirements.txt than
# from `pip install -e .` against a hatchling-backed pyproject.

# ── Web framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── Database ──
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
aiosqlite>=0.20.0
alembic>=1.14.0

# ── Redis ──
redis[hiredis]>=5.2.0

# ── Celery ──
celery[redis]>=5.4.0

# ── Vector search ──
pgvector>=0.3.0

# ── Embeddings / reranking ──
rank-bm25>=0.2.2
numpy>=1.26.0

# ── RAGAs evaluation ──
ragas>=0.2.0

# ── LLM providers ──
openai>=1.58.0
anthropic>=0.40.0
google-generativeai>=0.8.0
google-genai>=1.0.0
httpx>=0.28.0

# ── Observability ──
langfuse>=2.56.0

# ── Utilities ──
python-multipart>=0.0.18
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
tenacity>=9.0.0
